version: '3.2'

services:

  localtunnel:
    build:
      dockerfile: Dockerfile
      context: .
    restart: always
    command: " --local-host <your_host> --port <your_port> --subdomain <your_subdomain>"
